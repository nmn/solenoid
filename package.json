{
  "name": "solenoid",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.2.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.6"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "format": "biome format --write ."
  },
  "workspaces": [
    "tools/*",
    "packages/*"
  ]
}
